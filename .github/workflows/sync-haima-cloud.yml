name: Sync haima-cloud cask

on:
  schedule:
    - cron: "14 0,6,12,18 * * *"
  workflow_dispatch:

jobs:
  sync:
    permissions:
      contents: write
    uses: ./.github/workflows/_sync-cask.yml
    with:
      cask: haima-cloud
