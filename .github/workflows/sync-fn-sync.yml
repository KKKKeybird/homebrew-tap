name: Sync fn-sync cask

on:
  schedule:
    - cron: "7 0,6,12,18 * * *"
  workflow_dispatch:

jobs:
  sync:
    permissions:
      contents: write
    uses: ./.github/workflows/_sync-cask.yml
    with:
      cask: fn-sync
