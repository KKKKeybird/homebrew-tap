name: Sync scriptplayerplus cask

on:
  schedule:
    - cron: "35 0,6,12,18 * * *"
  workflow_dispatch:

jobs:
  sync:
    permissions:
      contents: write
    uses: ./.github/workflows/_sync-cask.yml
    with:
      cask: scriptplayerplus
